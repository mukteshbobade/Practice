 for j in range(0,n-1):
        for i in range(0,n-1):

            if arr[i]>arr[i+1]:
                arr[i],arr[i+1]=arr[i+1],arr[i]
                count=count+1
                #print(arr)
    return(count)
